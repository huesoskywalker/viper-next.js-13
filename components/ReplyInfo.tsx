export default async function ReplyInfo({
    replyId,
    replyText,
}: {
    replyId: string
    replyText: string
}) {
    // console.log(replyId)
    // console.log(`------------`)
    // console.log(replyText)
    return (
        <div>
            <h1 className="text-gray-300">Hello from ReplyInfo</h1>
        </div>
    )
}
